import { CorrectAnswerPipe } from './correct-answer.pipe';

describe('CorrectAnswerPipe', () => {
  it('create an instance', () => {
    const pipe = new CorrectAnswerPipe();
    expect(pipe).toBeTruthy();
  });
});
